import { ConverFileSizePipe } from './conver-file-size.pipe';

describe('ConverFileSizePipe', () => {
  it('create an instance', () => {
    const pipe = new ConverFileSizePipe();
    expect(pipe).toBeTruthy();
  });
});
